<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.practice.e_centrar">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".NavigationActivity" />
        <activity android:name=".webActivity" />
        <activity android:name=".CustomerActivity">

        </activity>
        <activity android:name=".CustomerListAvtivity" />
        <activity android:name=".DetailsCustomerActivity" />
        <activity android:name=".GetProductFromAPI" />
        <activity android:name=".ProductGridViewActivity" />
        <activity android:name=".imagesproductActivity" />
        <activity android:name=".ProductDetails" />
        <activity android:name=".gActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".OrderLineActivity" />
        <activity
            android:name=".EntryActivity"
            android:label="@string/title_activity_entry"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>